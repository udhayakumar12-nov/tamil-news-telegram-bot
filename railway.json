{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python news_bot_with_db.py",
    "restartPolicyType": "ON_FAILURE"
  }
}